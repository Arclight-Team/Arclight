#version 330 core
out vec4 color;

in vec2 uv;
uniform sampler2D screenTexture;


void main(){    
    color = texture(screenTexture, uv).rgba;
    //color = vec4(vec3(1.0) - texture(screenTexture, uv).rgb, 1.0);
}